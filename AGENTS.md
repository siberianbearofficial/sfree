The primary API is located in `api-go`. The `api-python-archived` directory contains the archived Python backend and should not be modified.

Avoid leaving comments in code unless they clarify complex logic or are required by tools such as linters or Swagger.
