# Archived — Python API

This directory contains the original Python backend for SFree. It is no longer
maintained and is kept only for historical reference.

The active backend is `api-go/`. All new work should target that directory.
